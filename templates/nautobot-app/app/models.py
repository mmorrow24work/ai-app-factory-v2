"""App data models for {{PROJECT_NAME}}.

Empty on purpose -- a fresh scaffold has no domain model yet. Subclass Nautobot's own base
model classes (e.g. `nautobot.apps.models.PrimaryModel`) here rather than plain Django models,
per CLAUDE.md's conventions. Ship each model's migration alongside it.
"""
