"""Nav menu items for {{PROJECT_NAME}}.

Empty on purpose -- populate `menu_items` with `nautobot.apps.ui.NavMenuTab`/`NavMenuGroup`/
`NavMenuItem` entries once there are views worth linking to.
"""

menu_items = ()
