"""URL routing for {{PROJECT_NAME}}'s UI views.

Empty on purpose -- register routes here (and via a router in `api/urls.py` for the REST API)
as views are added.
"""

from django.urls import path

app_name = "plugin"
urlpatterns: list[path] = []
