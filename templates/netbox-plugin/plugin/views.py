"""Views for {{PROJECT_NAME}}.

Empty on purpose -- add views here as models are added, subclassing NetBox's own base view
classes (e.g. `netbox.views.generic.ObjectView`) rather than plain Django views.
"""
