"""Filter sets for {{PROJECT_NAME}}.

Named `filtersets.py`, not `filters.py` -- that's NetBox's own convention, unlike Nautobot's.
Empty on purpose -- add a `netbox.filtersets.NetBoxModelFilterSet` subclass per model as models
are added.
"""
