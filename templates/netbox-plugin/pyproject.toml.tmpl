# See PEP 518 for the spec of this file
# https://www.python.org/dev/peps/pep-0518/

[build-system]
requires = ["setuptools>=77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "{{APP_NAME}}"
version = "0.1.0"
authors = [
    {name = "{{AUTHOR_NAME}}"},
]
description = "{{PROJECT_DESCRIPTION}}"
readme = "README.md"
keywords = ["netbox", "netbox-plugin"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Framework :: Django",
    "Intended Audience :: Developers",
    "Intended Audience :: System Administrators",
    "Intended Audience :: Telecommunications Industry",
    "Natural Language :: English",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.12",
    "Topic :: System :: Networking",
]
requires-python = ">=3.12.0"

[project.optional-dependencies]
test = [
    "ruff",
    "pre-commit",
    "pytest",
]

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "B",  # flake8-bugbear
    "C4", # flake8-comprehensions
    "UP", # pyupgrade
]
ignore = [
    "E501", # line too long, handled by ruff-format
    "B008", # do not perform function calls in argument defaults
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]

[tool.setuptools.packages.find]
where = ["."]
# NOTE: the actual directory shipped by the template is literally named `plugin` --
# factory-new.sh only substitutes double-curly placeholder tokens inside file content, never
# directory/file names. If {{PYTHON_PACKAGE}} is not `plugin`, rename the directory (and
# update this include and package-data glob below) before relying on this.
include = ["{{PYTHON_PACKAGE}}*"]

[tool.setuptools.package-data]
{{PYTHON_PACKAGE}} = [
    "templates/**",
    "static/**",
]
